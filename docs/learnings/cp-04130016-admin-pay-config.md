### 根本原因

小程序 `checkAdmin` 依赖 `admins` 云数据库集合动态查询，需要通过外部脚本写入管理员 OpenID，同时缺少支付配置指南和运维工具云函数。

### 下次预防

- [ ] 新建云函数时同步创建 `package.json`（wx-server-sdk 依赖）
- [ ] 配置类文档（凭据格式、步骤）统一放 `docs/` 目录，避免散落
- [ ] 管理员 OpenID 等敏感配置通过脚本写入，不硬编码进云函数代码
